FIND_PACKAGE(Lua)
IF ( LUA_FOUND )
  STRING( COMPARE EQUAL LUA_VERSION_MAJOR "5" OK1 )
  STRING( COMPARE GREATER_EQUAL LUA_VERSION_MINOR "3" OK2 )
  IF ( OK1 AND OK2 )
    ADD_DEFINITIONS( -DUSE_SYSTEM_LUA )
  ELSE()
    SET( LUA_FOUND false )
  ENDIF()
ENDIF()
